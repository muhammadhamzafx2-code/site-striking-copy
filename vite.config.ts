import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig({
  vite: {
    base: "/site-striking-copy/", // Replace 'your-repo-name' with your actual GitHub repository name
  },
});
